# Zeref — local development environment variables (NO SECRETS)
# Copy to .env and adjust for your machine.

POSTGRES_DB=zeref
POSTGRES_USER=zeref
POSTGRES_PASSWORD=zeref
POSTGRES_PORT=5432
DATABASE_URL=postgresql://zeref:zeref@localhost:5432/zeref